# PassGenerator
A powerful and secure password generator written in Python. Generates strong passwords with uppercase, lowercase, digits, and only the allowed special characters (&amp;, @, #, *, !, %).
